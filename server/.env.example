# Copy to .env and fill with your GitHub OAuth app credentials
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret
# The origin where your IDE is served (e.g., http://localhost:8080)
APP_ORIGIN=http://localhost:8080
# Server port
PORT=4000
